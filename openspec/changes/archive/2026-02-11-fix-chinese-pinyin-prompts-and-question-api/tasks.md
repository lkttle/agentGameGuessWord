## 1. 中文拼音猜词文案改造

- [x] 1.1 盘点 `src/app/play/page.tsx` 中与玩法理解相关的标题、说明、示例文案，替换为“拼音首字母猜中文词语”语义。
- [x] 1.2 盘点 `src/app/room/[roomId]/page.tsx` 中猜词标签、输入占位和错误提示，移除英文单词导向表达。
- [x] 1.3 增加明确示例（如 `CF` 对应“吃饭/充分/出发”）并确认用户在进入房间前后都可看到规则提示。

## 2. 题库与数据结构建设

- [x] 2.1 新增题目数据结构定义（包含 `initials`、`initialsText`、`answer`、`category`）。
- [x] 2.2 新增本地题库模块（覆盖 2/3/4 字词语，含动作、动物、水果等分类）。
- [x] 2.3 为题库添加筛选与随机抽样能力，支持按字数、分类和数量取题。

## 3. 题目校验与生成逻辑

- [x] 3.1 实现题目校验器，校验答案长度必须为 2-4 且为中文词语。
- [x] 3.2 实现首字母映射校验，确保 `initials.length` 与答案字数一致且每项为单个大写字母。
- [x] 3.3 实现 `initialsText` 规范化生成，保证其等于 `initials` 按顺序拼接结果。

## 4. 题目生成接口

- [x] 4.1 新增 `src/app/api/questions/generate/route.ts`，提供题目生成 API。
- [x] 4.2 支持可选请求参数：`length`、`category`、`count`，并实现参数边界校验。
- [x] 4.3 定义成功响应格式，返回 `questions` 数组及结构化题目字段。
- [x] 4.4 定义空结果/非法参数错误语义（如 `422`/`400`），保证调用方可区分失败原因。

## 5. 验证与交付

- [x] 5.1 增加最小验证用例（或自测脚本）覆盖：默认生成、按长度筛选、按分类筛选、无结果报错。
- [x] 5.2 进行接口联调自测并记录示例请求/响应，确认题目必含首字母、答案与分类。
- [x] 5.3 复查前端页面关键文案，确认不再出现“猜英文单词”误导表述。
